<!-- agent-docs:start -->
## Git 操作の制約

- `git add` によるステージングを行ってはならない
- `git commit` によるコミットを行ってはならない
- `git rebase` を使用してはならない

ただし、`claude/*` ブランチで作業している場合は `git add` と `git commit` の制約を適用しない。
`git rebase` の禁止はすべてのブランチで適用する。

## Git メッセージの言語

- コミットメッセージは英語で書くこと
- プルリクエストのタイトルと説明は日本語で書くこと

## GitHub 操作

- プルリクエストやイシューを参照・操作するときは `/github` スキルを使用すること

## 実装スキルの使い分け

実装・修正タスクでスキルの明示指定がない場合は、原則として `/dev` スキルを使用すること。

- `/dev`（既定）: 1 PR で完結する規模の機能実装・変更
- `/fix`: 既存実装への指摘対応・タイポ・文言などの局所修正
- `/pm`: 複数機能にまたがる・設計判断が重い・並列実装が必要な大規模実装（`/dev` 実行中に規模超過を検知した場合も `/pm` を提案する）

## コーディング規約

- コード実装時は `docs/rules/` 配下のルールファイルを参照し、必ず準拠すること

## 回答フォーマット

質問・議論への回答にのみ適用（実装の進捗・成果報告には適用しない）。

回答は以下のセクションを優先度順に、必要なものだけ組み立てる。
各セクションは `## セクション名:` の見出しを付けて区切り、不要なセクションは省略する。

- **結論**（必須）: 最初に答え・要点を1〜2文で述べる。前置き・背景から始めない。
- **前提**（要求が曖昧なとき）: 解釈が割れる点・スコープを示す。結論を断定できないときは結論より前に置いてよい。
- **選択肢**（複数案があるとき）: 案ごとのトレードオフを並べる。推奨案を先頭に明示する。
- **根拠**（理由が自明でないとき）: なぜその結論かを箇条書きで。
- **詳細**（深掘り・実行が必要なとき）: 手順・コード・補足。これらが答えそのものなら説明を最小化し主役にする。
- **確認**（判断を委ねるとき）: ユーザーへの質問を末尾に置く。

出力例:

```md
## 結論:
A を採用すべき。

## 根拠:
- B だから
- C のケースでも D で対応できる

## 詳細:
1. ...
2. ...
```

禁止: 定型の前置き／謝辞、聞かれていない周辺知識、直前内容の繰り返し。
質問の粒度に長さを合わせる。

（詳細は `docs/response.md`）
<!-- agent-docs:end -->
