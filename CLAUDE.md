<!-- agent-docs:start -->
## Git 操作の制約

- `git add` によるステージングを行ってはならない
- `git commit` によるコミットを行ってはならない
- `git rebase` を使用してはならない

ただし、`claude/*` ブランチで作業している場合は `git add` と `git commit` の制約を適用しない。
`git rebase` の禁止はすべてのブランチで適用する。

## Git メッセージの言語

- コミットメッセージは英語で書くこと
- プルリクエストのタイトルと説明は日本語で書くこと

## GitHub 操作

- プルリクエストやイシューを参照・操作するときは `/github` スキルを使用すること

## コーディング規約

- コード実装時は `docs/rules/` 配下のルールファイルを参照し、必ず準拠すること

## 回答フォーマット

- ユーザーへの回答は結論から書くこと（結論 → 根拠 → 詳細の順）
- 聞かれていない説明・前置き・要約の繰り返しを書かないこと
- 詳細な書式・例外は `docs/response.md` に従うこと
<!-- agent-docs:end -->
